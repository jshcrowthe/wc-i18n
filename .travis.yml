language: node_js
dist: trusty
sudo: required
node_js: stable
addons:
  firefox: latest
  chrome: stable
script:
  - xvfb-run npm test
  - 'if [ "${TRAVIS_PULL_REQUEST}" = "false" ]; then wct --module-resolution=node --npm -s ''default''; fi'